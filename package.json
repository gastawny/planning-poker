{
  "name": "planning-poker",
  "private": true,
  "type": "module",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "bun run --filter '*' dev",
    "typecheck": "bun run --filter '*' typecheck",
    "lint": "bunx biome ci ."
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "shadcn": "^4.4.0",
    "typescript": "^5.8.3"
  }
}
